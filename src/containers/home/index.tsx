import React, {useEffect, useCallback} from 'react'
import Videos from '../videos'

const HomePage = () => {
    return (
        <React.Fragment>
            <Videos />
        </React.Fragment>
    )
}

export default HomePage
